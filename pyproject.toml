[project]
name = "cyclingroute-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "fastmcp[apps]>=3.2.4",
    "gpxpy>=1.6.2",
    "httpx>=0.28.1",
]
